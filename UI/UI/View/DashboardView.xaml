<UserControl x:Class="UI.View.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.View"
             xmlns:properties="clr-namespace:UI.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1400">
   
    <Grid Background="{StaticResource panelActiveColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="White" BorderThickness="1" Margin="10" Grid.Column="0">
            <Grid Width="1400">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,-20,0,0" Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <Label Content="{Binding Source={x:Static properties:Strings.DashBoardProductLabel}}" Style="{StaticResource LabelWithWhiteBackground}"/>
                    <TextBox   Style="{StaticResource TextboxWithWhiteBackground}"/>
                </StackPanel>
                <Border BorderBrush="White" BorderThickness="1" Grid.Row="1" Margin="10,-10,10,10" Width="600" HorizontalAlignment="Left">
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="10" Orientation="Vertical" Width="600">
                        <Label Content="{Binding Source={x:Static properties:Strings.DashboardMachineLogLabel}}" HorizontalAlignment="Left" Style="{StaticResource HeaderLabelWithWhiteBackground }"/>
                        <ListBox Style="{StaticResource ListboxWithWhiteBackground}" HorizontalAlignment="Left" Margin="5"/>
                    </StackPanel>
                </Border>
                <Border BorderBrush="White" BorderThickness="1" Grid.Row="2" Margin="10" Width="600" HorizontalAlignment="Left">
                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,0" Orientation="Vertical">
                        <Label Content="{Binding Source={x:Static properties:Strings.DashboardMachineLogCountLabel}}" Style="{StaticResource HeaderLabelWithWhiteBackground}"/>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="{Binding Source={x:Static properties:Strings.DashboardTotalCountLabel}}" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelWithWhiteBackground}"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource TextboxWithWhiteBackground}"/>
                            <Label Content="{Binding Source={x:Static properties:Strings.DashboardTossingLabel}}" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelWithWhiteBackground}"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource TextboxWithWhiteBackground}"/>
                            <Label Content="{Binding Source={x:Static properties:Strings.DashboardOKCount}}" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelWithWhiteBackground}"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource TextboxWithWhiteBackground}"/>
                            <Label Content="{Binding Source={x:Static properties:Strings.DashboardCycleTimeLabel}}" Grid.Row="0" Grid.Column="2" Style="{StaticResource LabelWithWhiteBackground}"/>
                            <TextBox Grid.Row="0" Grid.Column="3" Style="{StaticResource TextboxWithWhiteBackground}"/>
                            <Label Content="{Binding Source={x:Static properties:Strings.DashboardLeadTime}}" Grid.Row="1" Grid.Column="2" Style="{StaticResource LabelWithWhiteBackground}"/>
                            <TextBox Grid.Row="1" Grid.Column="5" Style="{StaticResource TextboxWithWhiteBackground}"/>

                        </Grid>
                    </StackPanel>
                </Border>
                <Border BorderBrush="White" BorderThickness="1" Grid.Row="3" Margin="10" Width="600" HorizontalAlignment="Left">

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="10" Orientation="Vertical">
                        <Label Content="{Binding Source={x:Static properties:Strings.DashboardLogLabel}}" HorizontalAlignment="Left" Margin="5" Foreground="Black" Style="{StaticResource HeaderLabelWithWhiteBackground}"/>
                        <ListBox Style="{StaticResource ListboxWithWhiteBackground}" Height="200" HorizontalAlignment="Left" Margin="5"/>
                    </StackPanel>
                </Border>
            </Grid>

           
        </Border>
        <Border BorderBrush="White" BorderThickness="1" Margin="10" Grid.Row="0" Grid.Column="1">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                     

                        <StackPanel Grid.Row="0" Grid.Column="0"  Orientation="Vertical">
                        <Label Content="{Binding Source={x:Static properties:Strings.DashboardStaticIndicatorsLabel}}" Style="{ StaticResource HeaderLabelWithWhiteBackground}"/>
                            <ListBox Style="{StaticResource ListboxWithWhiteBackground}" HorizontalAlignment="Left" Margin="5"/>
                        </StackPanel>
                        <StackPanel  Grid.Row="0" Grid.Column="1" Orientation="Vertical" >
                        <Label Content="{Binding Source={x:Static properties:Strings.DashboardHive}}" Style="{StaticResource HeaderLabelWithWhiteBackground}"/>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,50,0,0">
                            <Button Content="{Binding Source={x:Static properties:Strings.DashboardCommErrorButton}}" Style="{StaticResource ButtonboxWithWhiteBackground}" Background="Green" Height="40"/>
                            <Button Content="{Binding Source={x:Static properties:Strings.DashboardRepairButtonLabel}}" Style="{StaticResource ButtonboxWithWhiteBackground}" Background="Orange" Height="40"/>
                            </StackPanel>
                        </StackPanel>
                </Grid>

                <Grid Grid.Row="1">
                    <Border BorderBrush="White" BorderThickness="1" Margin="5">

                        <StackPanel Grid.Row="0" Grid.Column="0"  Orientation="Vertical">
                            <Label Content="{Binding Source={x:Static properties:Strings.DashboardWorkingAnimationLabel}}" Style="{ StaticResource HeaderLabelWithWhiteBackground}" Height="30"/>
                            <ListBox Style="{StaticResource ListboxWithWhiteBackground}" HorizontalAlignment="Left" Margin="5"/>
                        </StackPanel>
                    </Border>
                </Grid>
                <Grid Grid.Row="2">
                    <Border BorderBrush="White" BorderThickness="1" Margin="5">
                    <StackPanel Orientation="Vertical">
                            <Label Content="{Binding Source={x:Static properties:Strings.DashboardControlsLabel}}" Style="{StaticResource HeaderLabelWithWhiteBackground}"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="{Binding Source={x:Static properties:Strings.DashboardStartButton}}" Command="{Binding StartCommand}" Margin="5" Style="{StaticResource BigButtonboxWithWhiteBackground }" Background="Green"/>
                                <Button Content="{Binding Source={x:Static properties:Strings.DashboardPauseButton}}" Command="{Binding PauseCommand}" Margin="5"  Style="{StaticResource BigButtonboxWithWhiteBackground }" Background="#79C2D0"/>
                                <Button Content="{Binding Source={x:Static properties:Strings.DashboardStopButton}}" Command="{Binding StopCommand}" Margin="5"  Style="{StaticResource BigButtonboxWithWhiteBackground }" Background="Red"/>
                                <Button Content="{Binding Source={x:Static properties:Strings.DashboardResetButton}}" Command="{Binding ResetCommand}" Margin="5"  Style="{StaticResource BigButtonboxWithWhiteBackground }" Background="#A0C8D0"/>
                        </StackPanel>
                        <!--<Image Source="/Resources/Images/animation.gif" Height="100" Margin="10"/>-->
                    </StackPanel>
                    </Border>

                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
