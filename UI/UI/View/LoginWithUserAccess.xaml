<UserControl x:Class="UI.View.LoginWithUserAccess"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:properties="clr-namespace:UI.Resources"
             xmlns:local="clr-namespace:UI.View"
             mc:Ignorable="d" Width="350" Height="170"
            >
   
    <Border BorderBrush="White" BorderThickness="1">

        <Grid Background="{StaticResource panelActiveColor}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="{Binding Source={x:Static properties:Strings.LoginWithUserAccessAccess}}" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelWithWhiteBackground}"/>
            <Label Content="{Binding Source={x:Static properties:Strings.LoginWithUserAccessAccessUserName}}" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelWithWhiteBackground}"/>
            <Label Content="{Binding Source={x:Static properties:Strings.LoginWithUserAccessAccessPassword}}" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelWithWhiteBackground}"/>
            <ComboBox Grid.Row="0" Grid.Column="1" Style="{StaticResource ComboBoxWithWhiteBackground}">
                <ComboBox.Items>
                    <ComboBoxItem Content="{Binding Source={x:Static properties:Strings.LoginWithUserAccessAccessCBItemAdministrator}}" Style="{StaticResource ComboBoxItemsWithWhiteBackground}"/>
                    <ComboBoxItem Content="{Binding Source={x:Static properties:Strings.LoginWithUserAccessAccessCBItemGuest}}" Style="{StaticResource ComboBoxItemsWithWhiteBackground}"/>
                    <ComboBoxItem Content="{Binding Source={x:Static properties:Strings.LoginWithUserAccessAccessCBItemTechnicial}}" Style="{StaticResource ComboBoxItemsWithWhiteBackground}"/>
                    <ComboBoxItem Content="{Binding Source={x:Static properties:Strings.LoginWithUserAccessAccessCBItemUser}}" Style="{StaticResource ComboBoxItemsWithWhiteBackground}"/>
                </ComboBox.Items>
            </ComboBox>
            <TextBox  Grid.Row="1" Grid.Column="1" Style="{StaticResource TextboxWithWhiteBackground}"/>
            <TextBox  Grid.Row="2" Grid.Column="1" Style="{StaticResource TextboxWithWhiteBackground}"/>
            <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Cancel"  Margin="0,5,0,5" Style="{StaticResource  ButtonboxWithWhiteBackground}" />
                <Button Content="Submit"  Margin="5,5,0,5" Style="{StaticResource  ButtonboxWithWhiteBackground}" Command="{Binding _loginComman, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
